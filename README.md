# stegodata
A lightweight, format-agnostic library to embed and extract structured JSON payloads directly inside binary files (PDFs, PNGs, etc.) via trailing data. Turn any media file into a self-describing container without breaking visual rendering or heavy database schemas.
